{
  "name": "@bun-monorepo-template/new",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "postinstall": "bun run scripts/postinstall.ts",
    "lint": "oxlint --config oxlint.json --fix .",
    "format": "prettier --config prettier.config.cjs --write .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "bunup",
    "test": "bun test tests/scaffold.test.ts tests/templates.test.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "latest",
    "@bun-monorepo-template/qa": "workspace:*",
    "bunup": "latest",
    "clerc": "1.2.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
