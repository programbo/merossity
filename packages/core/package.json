{
  "name": "@merossity/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    "./api": "./src/api/index.ts",
    "./meross": "./src/meross/index.ts",
    "./meross/inventory": "./src/meross/inventory.ts"
  },
  "scripts": {
    "lint": "oxlint --config oxlint.json --fix .",
    "format": "prettier --config prettier.config.cjs --write .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "build": "bunup",
    "dev": "bunup --watch"
  },
  "devDependencies": {
    "@merossity/qa": "workspace:*",
    "@types/bun": "latest",
    "@types/node": "latest",
    "bunup": "latest",
    "concurrently": "latest"
  }
}
